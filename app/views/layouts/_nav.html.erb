<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "EventHall", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">City <span class="caret"></span></a>
            <ul class="dropdown-menu">
                <% City.all.each do |c| %>
                    <li class="<%= 'active' if params[:city] == c.name %>">
                      <%= link_to c.name, halls_path(city: c.name ) %>
                    </li>
                <% end %>
            </ul>
        </li>

        <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Venue Type <span class="caret"></span></a>
          <ul class="dropdown-menu">
              <% VenueType.all.each do |c| %>
                  <li class="<%= 'active' if params[:venue_type] == c.name %>">
                    <%= link_to c.name, halls_path(venue_type: c.name ) %>
                  </li>
              <% end %>
          </ul>
        </li>

        <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Event Type <span class="caret"></span></a>
          <ul class="dropdown-menu">
              <% EventType.all.each do |c| %>
                  <li class="<%= 'active' if params[:event_type] == c.name %>">
                    <%= link_to c.name, halls_path(event_type: c.name ) %>
                  </li>
              <% end %>
          </ul>
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :get %></li>
          <li><%= link_to "Edit Profile (#{current_user.name})", edit_user_registration_path %></li>
        <% else %>
          <li><%= link_to "Log In", new_user_session_path %></li>
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>